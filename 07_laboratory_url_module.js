// const url = require('url');

// const myUrl = 'http://www.example.com:8080/pathname?name=JohnDoe#fragment';
// const parsedUrl = url.parse(myUrl, true);

// console.log(parsedUrl);
// console.log(parsedUrl.query.name);

const url = require('url');

const urlString = 'https:                                                                 

const parsedUrl = url.parse(urlString, true);

console.log('//www.example.com:8080/products?category=electronics&sort=price';

const parsedUrl = url.parse(urlString, true);

console.log('URL Components:');
console.log(Protocol: ${parsedUrl.protocol});
console.log(Hostname: ${parsedUrl.hostname});
console.log(Port: ${parsedUrl.port});
console.log(Pathname: ${parsedUrl.pathname});
console.log(Query Parameters: ${parsedUrl.query});
console.log(Query Parameters (as object):);
console.log(parsedUrl.query);
